import QtQuick
import QtQuick.Controls

Rectangle {
    width: parent.width
    height: parent.height
    color: "transparent"

    Text {
        anchors.centerIn: parent
        text: "Раздел Настройки"
        font.pixelSize: 24
        color: "#555"
    }
}
